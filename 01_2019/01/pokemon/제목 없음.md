

1

```python
d = {'a': 1, 'b': 2}
a1 = d.update(c=3)
a2 = a1

len(a1) 은 2 다.
a1 와 a2 는 같은 딕셔너리를 가리키고 있다.
이 코드는 에러가 발생한다.
보기 중에는 답이 없다.
```

2 

```python
my_complex = 3+4j
my_list = [1, 2, 3]
my_dict = {1: 1, 2: 2}

my_complex.real()
my_list.sort()
my_list.pop()
my_dict.keys()
```

3

```python
print(type({ 'a': 'apple' }))

dict
```

4

```python
my_int = 3

isinstance(int, my_int)
my_int == 3
isinstance(my_int, 3)
type(my_int) == int
```

5

```python
d1 = {'d': dict()}
d2 = dict(d={})

d1 == d2 는 False 다.
len(d1) 은 1, len(d2) 는 0 이다.
id(d1) == id(d2) 는 True 다.
d1 의 value 와 d2 의 value 는 모두 비어있는 딕셔너리다.
```

6

```python
result = 4 + True + False + 5

print(result)

9
10
11
에러 발생
```

7

```python
name = 'hong'
class Person:
	name = 'choi'
	def greeting(self):
	print(name)

p1 = Person()
p1.name = 'kim'
p1.greeting()

kim
choi
hong
None
```

8

```python
class Person:
population = 0

    def __init__(self, name):
        self.name = name
        Person.population += 1

p1 = Person('hong')
p2 = Person('kim')
p3 = Person('kang')

0
1
2
3
```

9

```python
def func(a, b=1, c=2, *args, **kwargs):
    d = sum([n*2 for n in args if n > 2])
    e = sum([v*v for k, v in kwargs.items()])
    return a + b + c + d + e

print(func(9, 4, 2, 3, 1, 7, d=3, e=6))

80
81
82
83
```

10

```python
string = "i'm hungry"
result = ""
for idx,value in enumerate(string):
    if idx%2:
    	result += value.upper()
    else:
        result += value
    print(result)
    
I'M HUNGRY
i'm hUnGrY
I'M HuNgRy
오류 발생
```

11

```python
a = 1
def my_func_1():
	a = 5
	my_func_2()

def my_func_2():
	print(a, end='')

my_func_1()
print(a)


```

12

```python
def fib(n):
	if n == 0 or n==1:
		return 1
	else:
		return fib(n-1) + fib(n-2)

print(fib(4))

1번, 4번
2번, 3번
3번, 2번
4번, 1번
```

13

```python
함수의 인자(parameter)는 함수 선언시 설정한 값이며, 인수(argument)는 함수 호출시 넘겨주는 값이다.
함수는 오직 하나의 객체만 반환할 수 있다.
함수에서 return을 작성하지 않으면 코드 실행시 오류가 발생한다.
같은 이름의 함수를 선언하면, 함수 호출 시 나중에 선언된 함수가 실행된다.
```

14

```python
def func(c, b, a):
	return a*b+c

print(func(2, 5, 4))
```

15

```python
list, tuple, range, string, binary 같은 자료형 타입이 있다.
데이터의 순서대로 나열되어 있는 자료형이다.
in 연산자를 통해 특정 값이 시퀀스 자료형에 있는지 확인할 수 있다.
기본적으로 정렬이 되어 있지만, 사용자 임의대로 순서를 변경할 수 있다.
```

16

```python
def func(c='5', *args):
    a, c, b = args
	return a + b + c

print(func('3','4','1','2'))

341
412
421
314
```

17

```python
abs()
sqrt()
list()
map()
```

18

```python
s = "hello my name is ssafy"
	for i in s:
		if i == "m":
		print(s)

m
m
hello my name is ssafy
hello my name is ssafy
m m
hello y nae is ssafy
```

19

```python
L = [0, 0, 0, 0, 0]
L = [0] * 5
L = [] ; L.append(0*5)
L = [0 for i in range(5)]
```

20

```python
class Person:
    def __init__(self, name, age):
    	self.name = name

p1 = Person(p1, 'hong', 100)
p2 = Person('kim')
p3 = Person(age=3, name='kang')
p4 = Person()
```

21

```python
names = ['John', 'Ron', 'James', 'Betty']
print(names[-2][-2])

Betty
y
James
e
```

22

```python
import copy

list1 = [3, 'a', 'b']
list2 = [1, 2, list1]

list3 = list1[:]
list4 = copy.copy(list2)
list5 = copy.deepcopy(list2)

list1 == list3의 결과는 True이다.
list4[2][0] = 4
print(list2[2][0])의 결과는 4이다.
list4[2] = 5
print(list2[2])의 결과는 5이다.
list5[2][1] = 3
print(list2[2][1])의 결과는 'a'이다.
```

23

```python
fruits = { 'apple': '사과', 'banana': '바나나' }
a = fruits.get('apple')
b = fruits.get('cherry')
c = fruits.get('melon', True)
d = { a: b }
if c:
	print(d)
    
에러가 난다.
b 는 None 이다.
실행하면 아무것도 출력되지 않는다.
d 는 { 'apple': 'cherry' } 다.
```

24

```python
def my_sum(a, b):
    c = a + b
    print(c)

result = my_sum(5, 8)
```

25

```python
word = "python"

indexing = word[3:8]

print(indexing)
```



